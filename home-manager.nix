{ pkgs, config, ... }:
{
  # imports = [ <home-manager/nixos> ];
  # home-manager.useUserPackages = true;
  # home-manager.users = {
  #   # "${config._.user}" = {
  #   #   imports = [ ./home.nix ];
  #   # };
  #   root = {
  #     imports = [ ./home.nix ];
  #   };
  # };
}
